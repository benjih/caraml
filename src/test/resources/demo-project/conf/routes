# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /api/search                                       Application.search
GET     /api/artists                                      Application.getArtists
GET     /api/albums                                       Application.getAlbums

